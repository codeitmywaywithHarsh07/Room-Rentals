<nav class="navbar navbar-expand-md sticky-top bg-body-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <i class="fa-solid fa-location-arrow logo fs-3"></i>
        <span>RoomRentals.</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/listings">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/listings/new">New Listings</a>
          </li>
        </ul>

        <div class="navbar-nav ms-auto">
          <form class="d-flex" role="search">
            <input type="search" id="form1" class="form-control me-2 search-field" placeholder="Search Destinations.."/>
            <button type="button" class="btn btn-primary" data-mdb-ripple-init>
              <i class="fas fa-search"></i>
            </button>
          </form>
        </div>

        <!-- Signup \ login \ logout -->

        <ul class="navbar-nav ms-auto userAuth">
          <% if(flag){ %>
            <li>
              <div class="userDropdown">
                <i class="profileLogo fa-solid fa-circle-user"></i>
                <div class="userOptions show">
                    <div><form method="get" action="/favourites/<%= currUser._id %>"><button>Favorites</button></form></div>
                    <div>Logout</div>
                </div>
              </div>
            </li>
            <li class="nav-item">
              <b><a class="nav-link navLink" href="/logout">LogOut</a></b>
            </li>
          <% } else { %>
            <li class="nav-item">
              <b><a class="nav-link navLink active" aria-current="page" href="/signup">SignUp</a></b>
            </li>
            
            <li class="nav-item">
              <b><a class="nav-link navLink" href="/login">LogIn</a></b>
            </li>
          <% } %>          
        </ul>
      </div>
    </div>
  </nav>

  <script>
    let userOptions = document.querySelector('.userOptions');
    let profileBtn = document.querySelector('.profileLogo');
    console.log(userOptions.classList.show ? true : false)

    profileBtn.addEventListener('click',()=>{
      if(!userOptions.classList.contains('show'))
      {
        userOptions.classList.add('show');
      }
      else{
        userOptions.classList.remove('show');
      }
    });
  </script>